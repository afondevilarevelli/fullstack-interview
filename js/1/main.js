/*
Merge two sorted arrays into a sorted array.

Given two sorted arrays of integers, return another sorted array which will be merge of the two.

E.g.:
arr1: [1, 4, 5, 8]
arr2: [2, 4, 6, 8]

merge(arr1, arr2) -> [1, 2, 4, 5, 6, 8]
*/

function merge(arr1, arr2) {
  // Code here
  return [];
}

module.exports = merge;
